# Phase 9.1 – ownership-icons-01

The textual ownership badge is replaced with compact Material Symbols icons:

- User App: `person` (`\ue7fd`)
- Built-in / Catalog App: `menu_book` (`\ue865`)
- Hover tooltip: `Eigene App` or `Katalog-App`

Both origins are now always visible without taking much horizontal space.
